# Copy to .env.local and adjust. The dashboard talks directly to the
# FastAPI backend; NEXT_PUBLIC_* vars are inlined at build time so they
# must be set before `npm run dev` / `npm run build`.

NEXT_PUBLIC_API_URL=http://127.0.0.1:8000
